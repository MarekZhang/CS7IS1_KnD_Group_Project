1) install requirements from requirements.txt
2) run command "python app.py" -- need python 3
3) open http://localhost:4040 to view the results of queries
4) Raw Queries are saved in Queries folder as .rq files